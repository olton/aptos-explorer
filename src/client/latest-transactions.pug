section.container-fluid
    div.container
        div.radius(data-role="panel" data-title-caption="LATEST TRANSACTIONS (<span id='latest-transactions-counter'>0</span>)" data-cls-title-caption="text-left")
            div.scrollable-container
                table.table.striped.row-border.subcompact.scrollable-table
                    thead
                        tr
                            th(style="width: 20px") St
                            th(style="width: 20px") Ty
                            th.text-center Version
                            th Hash
                            th Sender/Proposer
                            th Function
                            th.text-right Amount
                            th.text-right
                                span.mif-gas-station
                            th(style="width: 100px") Date

                    tbody#latest-transactions